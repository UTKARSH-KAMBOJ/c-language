#include<stdio.h>
int main(){
    int a;
    printf("enter the number :");
    scanf("%d",&a);
   switch(a){
    case 1:
    printf("the value is 1");
    break;
    case 2:
    printf("the value is 2");
    break;
default:
printf("the input is not a integer");

   }
   
    return 0;
}
