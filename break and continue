#include<stdio.h>
int main(){
    int num;
    printf("enter the value of n:");
    scanf("%d",&num);
    for(int i=1;i<=num;i++){
        if(i%5==0){
        continue;
    }
printf("%d\n",i);
    }

    return 0;
}
